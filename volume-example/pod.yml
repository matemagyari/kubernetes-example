apiVersion: v1
kind: Pod
metadata:
  name: volume-example-pod
  labels:
    app: volume-example
spec:
  containers:
    - name: volume-example
      image: volume-example:v1
      volumeMounts:
        - name: test-storage
          mountPath: /data/test
  volumes:
    - name: test-storage
      emptyDir: {}